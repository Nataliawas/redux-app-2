import {
    SHOW_NOTIFICATONS, HIDE_NOTIFICATIONS
} from './constants'

export const showNotifications = () => (
    {
        type: SHOW_NOTIFICATONS,
        message:
    }
)

export const hideNotifications = () => (
    {
        type: HIDE_NOTIFICATIONS
    }
)